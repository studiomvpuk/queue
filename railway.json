{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '[railway] pwd='$(pwd) && ls dist/ && test -f dist/main.js || (echo 'FATAL: dist/main.js not found' && exit 1) && npx prisma migrate deploy && exec node --enable-source-maps dist/main.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
